version: '3.8'

services:
  mongodb-rs0-p:
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: admin
    entrypoint:
      - bash
      - -c
      - |
        cp /keyfile /mongo_keyfile
        chmod 400 /mongo_keyfile
        chown 999:999 /mongo_keyfile
        exec docker-entrypoint.sh $$@
    command: "mongod --bind_ip_all --replSet rs0 --keyFile /mongo_keyfile --port 28018"
    volumes:
      - mongodb-rs0-p:/data/db
      - ./keyfile:/keyfile
    network_mode: "host"

  mongodb-rs0-s1:
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: admin
    entrypoint:
      - bash
      - -c
      - |
        cp /keyfile /mongo_keyfile
        chmod 400 /mongo_keyfile
        chown 999:999 /mongo_keyfile
        exec docker-entrypoint.sh $$@
    command: "mongod --bind_ip_all --replSet rs0 --keyFile /mongo_keyfile --port 28019"
    volumes:
      - mongodb-rs0-s1:/data/db
      - ./keyfile:/keyfile
    network_mode: "host"

  mongodb-rs0-s2:
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: admin
    entrypoint:
      - bash
      - -c
      - |
        cp /keyfile /mongo_keyfile
        chmod 400 /mongo_keyfile
        chown 999:999 /mongo_keyfile
        exec docker-entrypoint.sh $$@
    command: "mongod --bind_ip_all --replSet rs0 --keyFile /mongo_keyfile --port 28020"
    volumes:
      - mongodb-rs0-s2:/data/db
      - ./keyfile:/keyfile
    network_mode: "host"


  mongodb-rs1-p:
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: admin
    entrypoint:
      - bash
      - -c
      - |
        cp /keyfile /mongo_keyfile
        chmod 400 /mongo_keyfile
        chown 999:999 /mongo_keyfile
        exec docker-entrypoint.sh $$@
    command: "mongod --bind_ip_all --replSet rs1 --keyFile /mongo_keyfile --port 28021"
    volumes:
      - mongodb-rs1-p:/data/db
      - ./keyfile:/keyfile
    network_mode: "host"

  mongodb-rs1-s1:
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: admin
    entrypoint:
      - bash
      - -c
      - |
        cp /keyfile /mongo_keyfile
        chmod 400 /mongo_keyfile
        chown 999:999 /mongo_keyfile
        exec docker-entrypoint.sh $$@
    command: "mongod --bind_ip_all --replSet rs1 --keyFile /mongo_keyfile --port 28022"
    volumes:
      - mongodb-rs1-s1:/data/db
      - ./keyfile:/keyfile
    network_mode: "host"

  mongodb-rs1-s2:
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: admin
    entrypoint:
      - bash
      - -c
      - |
        cp /keyfile /mongo_keyfile
        chmod 400 /mongo_keyfile
        chown 999:999 /mongo_keyfile
        exec docker-entrypoint.sh $$@
    command: "mongod --bind_ip_all --replSet rs1 --keyFile /mongo_keyfile --port 28023"
    volumes:
      - mongodb-rs1-s2:/data/db
      - ./keyfile:/keyfile
    network_mode: "host"


volumes:
  mongodb-rs0-p:
  mongodb-rs1-p:
  mongodb-rs0-s1:
  mongodb-rs0-s2:
  mongodb-rs1-s1:
  mongodb-rs1-s2:

networks:
  messaging-network:
    driver: bridge

